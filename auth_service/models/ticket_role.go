// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TicketRole is an object representing the database table.
type TicketRole struct {
	TicketID   int       `boil:"ticket_id" json:"ticket_id" toml:"ticket_id" yaml:"ticket_id"`
	RoleName   string    `boil:"role_name" json:"role_name" toml:"role_name" yaml:"role_name"`
	ExpiryTerm null.Time `boil:"expiry_term" json:"expiry_term,omitempty" toml:"expiry_term" yaml:"expiry_term,omitempty"`

	R *ticketRoleR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L ticketRoleL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TicketRoleColumns = struct {
	TicketID   string
	RoleName   string
	ExpiryTerm string
}{
	TicketID:   "ticket_id",
	RoleName:   "role_name",
	ExpiryTerm: "expiry_term",
}

var TicketRoleTableColumns = struct {
	TicketID   string
	RoleName   string
	ExpiryTerm string
}{
	TicketID:   "ticket_role.ticket_id",
	RoleName:   "ticket_role.role_name",
	ExpiryTerm: "ticket_role.expiry_term",
}

// Generated where

type whereHelpernull_Time struct{ field string }

func (w whereHelpernull_Time) EQ(x null.Time) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_Time) NEQ(x null.Time) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_Time) LT(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_Time) LTE(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_Time) GT(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_Time) GTE(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

func (w whereHelpernull_Time) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_Time) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

var TicketRoleWhere = struct {
	TicketID   whereHelperint
	RoleName   whereHelperstring
	ExpiryTerm whereHelpernull_Time
}{
	TicketID:   whereHelperint{field: "`ticket_role`.`ticket_id`"},
	RoleName:   whereHelperstring{field: "`ticket_role`.`role_name`"},
	ExpiryTerm: whereHelpernull_Time{field: "`ticket_role`.`expiry_term`"},
}

// TicketRoleRels is where relationship names are stored.
var TicketRoleRels = struct {
	Ticket string
}{
	Ticket: "Ticket",
}

// ticketRoleR is where relationships are stored.
type ticketRoleR struct {
	Ticket *Ticket `boil:"Ticket" json:"Ticket" toml:"Ticket" yaml:"Ticket"`
}

// NewStruct creates a new relationship struct
func (*ticketRoleR) NewStruct() *ticketRoleR {
	return &ticketRoleR{}
}

func (r *ticketRoleR) GetTicket() *Ticket {
	if r == nil {
		return nil
	}
	return r.Ticket
}

// ticketRoleL is where Load methods for each relationship are stored.
type ticketRoleL struct{}

var (
	ticketRoleAllColumns            = []string{"ticket_id", "role_name", "expiry_term"}
	ticketRoleColumnsWithoutDefault = []string{"ticket_id", "role_name", "expiry_term"}
	ticketRoleColumnsWithDefault    = []string{}
	ticketRolePrimaryKeyColumns     = []string{"ticket_id", "role_name"}
	ticketRoleGeneratedColumns      = []string{}
)

type (
	// TicketRoleSlice is an alias for a slice of pointers to TicketRole.
	// This should almost always be used instead of []TicketRole.
	TicketRoleSlice []*TicketRole
	// TicketRoleHook is the signature for custom TicketRole hook methods
	TicketRoleHook func(context.Context, boil.ContextExecutor, *TicketRole) error

	ticketRoleQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	ticketRoleType                 = reflect.TypeOf(&TicketRole{})
	ticketRoleMapping              = queries.MakeStructMapping(ticketRoleType)
	ticketRolePrimaryKeyMapping, _ = queries.BindMapping(ticketRoleType, ticketRoleMapping, ticketRolePrimaryKeyColumns)
	ticketRoleInsertCacheMut       sync.RWMutex
	ticketRoleInsertCache          = make(map[string]insertCache)
	ticketRoleUpdateCacheMut       sync.RWMutex
	ticketRoleUpdateCache          = make(map[string]updateCache)
	ticketRoleUpsertCacheMut       sync.RWMutex
	ticketRoleUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var ticketRoleAfterSelectMu sync.Mutex
var ticketRoleAfterSelectHooks []TicketRoleHook

var ticketRoleBeforeInsertMu sync.Mutex
var ticketRoleBeforeInsertHooks []TicketRoleHook
var ticketRoleAfterInsertMu sync.Mutex
var ticketRoleAfterInsertHooks []TicketRoleHook

var ticketRoleBeforeUpdateMu sync.Mutex
var ticketRoleBeforeUpdateHooks []TicketRoleHook
var ticketRoleAfterUpdateMu sync.Mutex
var ticketRoleAfterUpdateHooks []TicketRoleHook

var ticketRoleBeforeDeleteMu sync.Mutex
var ticketRoleBeforeDeleteHooks []TicketRoleHook
var ticketRoleAfterDeleteMu sync.Mutex
var ticketRoleAfterDeleteHooks []TicketRoleHook

var ticketRoleBeforeUpsertMu sync.Mutex
var ticketRoleBeforeUpsertHooks []TicketRoleHook
var ticketRoleAfterUpsertMu sync.Mutex
var ticketRoleAfterUpsertHooks []TicketRoleHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TicketRole) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ticketRoleAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TicketRole) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ticketRoleBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TicketRole) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ticketRoleAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TicketRole) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ticketRoleBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TicketRole) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ticketRoleAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TicketRole) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ticketRoleBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TicketRole) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ticketRoleAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TicketRole) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ticketRoleBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TicketRole) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range ticketRoleAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTicketRoleHook registers your hook function for all future operations.
func AddTicketRoleHook(hookPoint boil.HookPoint, ticketRoleHook TicketRoleHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		ticketRoleAfterSelectMu.Lock()
		ticketRoleAfterSelectHooks = append(ticketRoleAfterSelectHooks, ticketRoleHook)
		ticketRoleAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		ticketRoleBeforeInsertMu.Lock()
		ticketRoleBeforeInsertHooks = append(ticketRoleBeforeInsertHooks, ticketRoleHook)
		ticketRoleBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		ticketRoleAfterInsertMu.Lock()
		ticketRoleAfterInsertHooks = append(ticketRoleAfterInsertHooks, ticketRoleHook)
		ticketRoleAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		ticketRoleBeforeUpdateMu.Lock()
		ticketRoleBeforeUpdateHooks = append(ticketRoleBeforeUpdateHooks, ticketRoleHook)
		ticketRoleBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		ticketRoleAfterUpdateMu.Lock()
		ticketRoleAfterUpdateHooks = append(ticketRoleAfterUpdateHooks, ticketRoleHook)
		ticketRoleAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		ticketRoleBeforeDeleteMu.Lock()
		ticketRoleBeforeDeleteHooks = append(ticketRoleBeforeDeleteHooks, ticketRoleHook)
		ticketRoleBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		ticketRoleAfterDeleteMu.Lock()
		ticketRoleAfterDeleteHooks = append(ticketRoleAfterDeleteHooks, ticketRoleHook)
		ticketRoleAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		ticketRoleBeforeUpsertMu.Lock()
		ticketRoleBeforeUpsertHooks = append(ticketRoleBeforeUpsertHooks, ticketRoleHook)
		ticketRoleBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		ticketRoleAfterUpsertMu.Lock()
		ticketRoleAfterUpsertHooks = append(ticketRoleAfterUpsertHooks, ticketRoleHook)
		ticketRoleAfterUpsertMu.Unlock()
	}
}

// One returns a single ticketRole record from the query.
func (q ticketRoleQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TicketRole, error) {
	o := &TicketRole{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for ticket_role")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TicketRole records from the query.
func (q ticketRoleQuery) All(ctx context.Context, exec boil.ContextExecutor) (TicketRoleSlice, error) {
	var o []*TicketRole

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TicketRole slice")
	}

	if len(ticketRoleAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TicketRole records in the query.
func (q ticketRoleQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count ticket_role rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q ticketRoleQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if ticket_role exists")
	}

	return count > 0, nil
}

// Ticket pointed to by the foreign key.
func (o *TicketRole) Ticket(mods ...qm.QueryMod) ticketQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`ticket_id` = ?", o.TicketID),
	}

	queryMods = append(queryMods, mods...)

	return Tickets(queryMods...)
}

// LoadTicket allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (ticketRoleL) LoadTicket(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTicketRole interface{}, mods queries.Applicator) error {
	var slice []*TicketRole
	var object *TicketRole

	if singular {
		var ok bool
		object, ok = maybeTicketRole.(*TicketRole)
		if !ok {
			object = new(TicketRole)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTicketRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTicketRole))
			}
		}
	} else {
		s, ok := maybeTicketRole.(*[]*TicketRole)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTicketRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTicketRole))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &ticketRoleR{}
		}
		args[object.TicketID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &ticketRoleR{}
			}

			args[obj.TicketID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`ticket`),
		qm.WhereIn(`ticket.ticket_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Ticket")
	}

	var resultSlice []*Ticket
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Ticket")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for ticket")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ticket")
	}

	if len(ticketAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Ticket = foreign
		if foreign.R == nil {
			foreign.R = &ticketR{}
		}
		foreign.R.TicketRoles = append(foreign.R.TicketRoles, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TicketID == foreign.TicketID {
				local.R.Ticket = foreign
				if foreign.R == nil {
					foreign.R = &ticketR{}
				}
				foreign.R.TicketRoles = append(foreign.R.TicketRoles, local)
				break
			}
		}
	}

	return nil
}

// SetTicket of the ticketRole to the related item.
// Sets o.R.Ticket to related.
// Adds o to related.R.TicketRoles.
func (o *TicketRole) SetTicket(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Ticket) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `ticket_role` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"ticket_id"}),
		strmangle.WhereClause("`", "`", 0, ticketRolePrimaryKeyColumns),
	)
	values := []interface{}{related.TicketID, o.TicketID, o.RoleName}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TicketID = related.TicketID
	if o.R == nil {
		o.R = &ticketRoleR{
			Ticket: related,
		}
	} else {
		o.R.Ticket = related
	}

	if related.R == nil {
		related.R = &ticketR{
			TicketRoles: TicketRoleSlice{o},
		}
	} else {
		related.R.TicketRoles = append(related.R.TicketRoles, o)
	}

	return nil
}

// TicketRoles retrieves all the records using an executor.
func TicketRoles(mods ...qm.QueryMod) ticketRoleQuery {
	mods = append(mods, qm.From("`ticket_role`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`ticket_role`.*"})
	}

	return ticketRoleQuery{q}
}

// FindTicketRole retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTicketRole(ctx context.Context, exec boil.ContextExecutor, ticketID int, roleName string, selectCols ...string) (*TicketRole, error) {
	ticketRoleObj := &TicketRole{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `ticket_role` where `ticket_id`=? AND `role_name`=?", sel,
	)

	q := queries.Raw(query, ticketID, roleName)

	err := q.Bind(ctx, exec, ticketRoleObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from ticket_role")
	}

	if err = ticketRoleObj.doAfterSelectHooks(ctx, exec); err != nil {
		return ticketRoleObj, err
	}

	return ticketRoleObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TicketRole) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ticket_role provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ticketRoleColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	ticketRoleInsertCacheMut.RLock()
	cache, cached := ticketRoleInsertCache[key]
	ticketRoleInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			ticketRoleAllColumns,
			ticketRoleColumnsWithDefault,
			ticketRoleColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(ticketRoleType, ticketRoleMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(ticketRoleType, ticketRoleMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `ticket_role` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `ticket_role` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `ticket_role` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, ticketRolePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into ticket_role")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.TicketID,
		o.RoleName,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ticket_role")
	}

CacheNoHooks:
	if !cached {
		ticketRoleInsertCacheMut.Lock()
		ticketRoleInsertCache[key] = cache
		ticketRoleInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TicketRole.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TicketRole) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	ticketRoleUpdateCacheMut.RLock()
	cache, cached := ticketRoleUpdateCache[key]
	ticketRoleUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			ticketRoleAllColumns,
			ticketRolePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update ticket_role, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `ticket_role` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, ticketRolePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(ticketRoleType, ticketRoleMapping, append(wl, ticketRolePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update ticket_role row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for ticket_role")
	}

	if !cached {
		ticketRoleUpdateCacheMut.Lock()
		ticketRoleUpdateCache[key] = cache
		ticketRoleUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q ticketRoleQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for ticket_role")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for ticket_role")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TicketRoleSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ticketRolePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `ticket_role` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ticketRolePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in ticketRole slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all ticketRole")
	}
	return rowsAff, nil
}

var mySQLTicketRoleUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TicketRole) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ticket_role provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(ticketRoleColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTicketRoleUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	ticketRoleUpsertCacheMut.RLock()
	cache, cached := ticketRoleUpsertCache[key]
	ticketRoleUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			ticketRoleAllColumns,
			ticketRoleColumnsWithDefault,
			ticketRoleColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			ticketRoleAllColumns,
			ticketRolePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert ticket_role, could not build update column list")
		}

		ret := strmangle.SetComplement(ticketRoleAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`ticket_role`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `ticket_role` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(ticketRoleType, ticketRoleMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(ticketRoleType, ticketRoleMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for ticket_role")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(ticketRoleType, ticketRoleMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for ticket_role")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for ticket_role")
	}

CacheNoHooks:
	if !cached {
		ticketRoleUpsertCacheMut.Lock()
		ticketRoleUpsertCache[key] = cache
		ticketRoleUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TicketRole record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TicketRole) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TicketRole provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), ticketRolePrimaryKeyMapping)
	sql := "DELETE FROM `ticket_role` WHERE `ticket_id`=? AND `role_name`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from ticket_role")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for ticket_role")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q ticketRoleQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no ticketRoleQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ticket_role")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ticket_role")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TicketRoleSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(ticketRoleBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ticketRolePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `ticket_role` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ticketRolePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ticketRole slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ticket_role")
	}

	if len(ticketRoleAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TicketRole) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTicketRole(ctx, exec, o.TicketID, o.RoleName)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TicketRoleSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TicketRoleSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ticketRolePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `ticket_role`.* FROM `ticket_role` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, ticketRolePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TicketRoleSlice")
	}

	*o = slice

	return nil
}

// TicketRoleExists checks if the TicketRole row exists.
func TicketRoleExists(ctx context.Context, exec boil.ContextExecutor, ticketID int, roleName string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `ticket_role` where `ticket_id`=? AND `role_name`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, ticketID, roleName)
	}
	row := exec.QueryRowContext(ctx, sql, ticketID, roleName)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if ticket_role exists")
	}

	return exists, nil
}

// Exists checks if the TicketRole row exists.
func (o *TicketRole) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TicketRoleExists(ctx, exec, o.TicketID, o.RoleName)
}
